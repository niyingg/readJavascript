// 合并对象

let desc, src, result;

// desc = {};
// src = { is: "src" };
// result = Object.assign(desc, src, { a: 1 });

// // 获取函数与设置函数

// dest = {
//   set a(val) {
//     console.log("sdsd");
//   },
// };

// src = {
//   get a() {
//     console.log("asa");
//   },
// };

// const res = Object.assign(dest, src);

// console.log(res === dest, dest);
// 会使用源对象上的get 取得属性的值，使用目标对象上的set 设置属性的值

// Object.is();

// 可计算属性
// 看到8.2
